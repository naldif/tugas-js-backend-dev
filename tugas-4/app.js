const { calculateCircleArea, squareArray } = require('./math');

// Example usage:
const radius = 5;
console.log(`Luas Lingkaran: ${calculateCircleArea(radius)}`);

const numbers = [1, 2, 3, 4];
console.log(`Array kuadrat: ${squareArray(numbers)}`);